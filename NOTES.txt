STRUCTURE

just-tune/
├─ node_modules/
├─ src/
│  ├─ assets/
│  ├─ components/
│  ├─ utils/
│  ├─ App.jsx (this is the main program)
│  └─ main.jsx
├─ package.json
└─ vite.config.js

# A list here of each elements input / output would make sense?
e.g. piano takes touch/mouse in, outputs MIDI note (in format ...)

# assets
- for fonts / images etc.

# components
- piano (piano keyboard user input)
- notation (staff display)
- partialselector (for partials selection by user)
- centdeviationcontrol (for user input cent deviation)
- playback (for audio)

# utils
- partials.js # for returning partial note names from fundamental (change to MIDI note in > freq. out??) + partial numbers + input cent deviation
